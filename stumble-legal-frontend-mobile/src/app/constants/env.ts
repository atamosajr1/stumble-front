import config from 'react-native-config';

export const ENV = config.ENV;
export const API_URL = config.API_URL;
export const IMAGE_AWS_URL = config.IMAGE_AWS_URL;
export const IMAGE_AWS_BUCKET = config.IMAGE_AWS_BUCKET;
export const STRIPE_PK = config.STRIPE_PK;
export const REFRESH_TOKEN_URL = '/api/v1/auth/token';
